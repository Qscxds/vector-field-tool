# 夜跑待决事项（2026-09-02 夜）

需要你拍板的事、被标记的失败测试、性能异常、工具安装情况。没有的项写「无」。

## 需要你拍板

- **`possible_continuum` 的判据是启发式的**：找到 ≥3 个点且 ≥60% 非双曲就报连续解集。一个真有多个退化平衡点的系统会被误报。课堂例子里没遇到，但你可能想改阈值或换成「点是否共线」的几何判据。
- **caveat 与工具结果摘要用中文、工具 description 用英文**。如果课程可能有英文学生，caveat 需要双语或改英文。
- **网页外壳把 mathjs 打进了浏览器包**（约几百 KB）。如果在意首屏体积，可以改为通过 `/mcp` 或一个内部 API 计算；今晚按「两个外壳共用 core」的最直接方式做。

## 被标记的失败测试

无。全部 155 个测试通过；没有任何一处通过放宽容差来让测试变绿（见 NIGHT-decisions.md 里两处「测试预期改正」的说明：一处是雅可比测试选错了奇异点，一处是 SDK 把校验错误包成 isError 结果而非 JSON-RPC 错误）。

## 性能

- 全套 `npm test` 约 3 秒。`analyze_system` 在 12×12 种子、20×20 网格下单次约 50 到 300 毫秒（工具层测试观察值），没有做专门的基准。

## 工具安装

- **playwright：未安装**，理由与替代方案见 NIGHT-decisions.md「D 阶段」。如果你想要可重复的浏览器冒烟，装好 playwright 后可以把那段脚本搬进 `scripts/`。

## 其他

- `b-tools-done` 所指的 commit 里混进了一个审查子智能体的临时探针测试文件（`lib/core/__probe__/probe.test.ts`），下一个 commit 已移除并加了 `.gitignore`。历史未改写。
- 后台的 lib/core 对抗式审查（子智能体）在 A 阶段结束时启动，结果见 NIGHT-summary.md「审查结论」一节；若那一节缺失，说明审查在我结束前没有返回。
